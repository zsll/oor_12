<p id="notice"><%= notice %></p>

<%= image_tag(@upload.file.url(:original)) %>
<%= image_tag(@upload.file.url(:large)) %>
<%= image_tag(@upload.file.url(:tailored)) %>

<%= link_to 'Edit', edit_upload_path(@upload) %> |
<%= link_to 'Back', uploads_path %>
